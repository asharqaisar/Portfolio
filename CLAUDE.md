# CLAUDE.md

@agent.md

Project rules, skills, and MCP configuration live in [`agent.md`](./agent.md) — read it in full before your first edit.

Installed skills: `ponytail` (+ review/audit/debt/gain/help) and `impeccable` in `.claude/skills/`, with slash commands in `.claude/commands/`.
MCP servers: `context7` (live docs) and `21st` (component search/retrieval) — see `.mcp.json`, check with `/mcp`.
